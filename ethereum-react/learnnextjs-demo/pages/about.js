import Layout2 from '../components/Layout.js'

const Pages = () => (<p>This is the about page</p>)
const content = (<p>This is the about page</p>)

export default () => (
		    <Layout2 content={Pages()}><p>This is the about page</p></Layout2>
		)
