const { interface, bytecode } = require('./compile');

const jsonparse = JSON.parse(interface);

console.log(jsonparse);
console.log('COMPARED TO');
console.log('COMPARED TO');
console.log('COMPARED TO');
console.log(interface);
